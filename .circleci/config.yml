version: 2
jobs:
  build:
    docker:
      - image: gcc:latest
    steps:
      - checkout
      - run:
          name: Make files
          command: |
            make
      - run:
          name: run tests
          command: |
            ./all_tests

